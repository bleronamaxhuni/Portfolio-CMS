[build]
buildCommand = "npm run build"

[deploy]
startCommand = "php artisan migrate --force && php artisan storage:link --force && php artisan config:cache && php artisan serve --host=0.0.0.0 --port=$PORT"
