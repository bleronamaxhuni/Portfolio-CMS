[build]
buildCommand = "npm run build"

[deploy]
startCommand = "php artisan migrate --force && php artisan storage:link --force && php artisan config:cache && php artisan queue:work --sleep=3 --tries=3 --max-time=3600 & exec php artisan serve --host=0.0.0.0 --port=$PORT"
